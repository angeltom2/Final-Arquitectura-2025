# Frontend - Konrad Gourmet (skeleton)

Tecnologías:
- React (Create React App minimal)
- Axios para llamadas HTTP
- Mediator simple (servicio) que encapsula llamadas a la API

Para iniciar localmente:
1. Copiar .env.example a .env y ajustar REACT_APP_API_URL
2. npm install
3. npm start

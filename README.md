# Sistema de Abastecimiento - Esqueleto del Proyecto

Este repositorio contiene la **estructura inicial** (skeleton) del proyecto **Sistema de Abastecimiento** para el restaurante *Konrad Gourmet*.
Incluye:
- Backend (Node.js + Express) con patrón **Mediator** básico.
- Frontend (React) con un servicio Mediator simple.
- Configuración con **Docker** y **docker-compose** que levanta: backend, frontend y MySQL.
- Archivos `.env`, `.gitignore`, `Dockerfile` y `docker-compose.yml`.

**Objetivo:** darte una base completa que puedas ejecutar por primera vez con Docker (instrucciones paso a paso se darán en la siguiente fase).

--- 

Estructura principal:
```
/backend
/frontend
/docker-compose.yml
.env (ejemplo)
.gitignore
README.md
```

Lee el archivo `.env` y ajusta credenciales antes de levantar los contenedores.
